---
title: SpotLight
description: An interactive spotlight effect component that follows mouse movement, creating an elegant hover illumination effect.
author: deep
published: true
date: 2025-07-27
slug: components/spot-light
---

<ComponentPreview name="spot-light" />

<InfoOut>The SpotLight component creates a beautiful spotlight effect that follows your mouse cursor, perfect for cards, buttons, and interactive elements that need an elegant hover state</InfoOut>

## Installation

<Tabs defaultValue="cli">
<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">
<CopyCode>
      {`npx shadcn@latest add "https://ace-ui-six.vercel.app/r/spotlight.json"`}
    </CopyCode>
</TabsContent>
<TabsContent value="manual">
<Steps>
<Step>Copy and paste the following code into your project.</Step>
<ComponentSource name="spot-light" />
</Steps>
</TabsContent>
</Tabs>

## Usage

<CopyCode>
  {`import { SpotLight } from "@/components/ui/spotlight";

export default function Example() {
  return (
    <div>
      <SpotLight
        className="h-52 w-40 flex justify-center items-center bg-black border border-white"
        spotlightColor="rgba(151, 151, 151, 0.1)"
      >
        Hover me
      </SpotLight>
    </div>
  );
}
`}
</CopyCode>


## Features

-  Smooth spotlight effect that follows mouse movement
-  Customizable spotlight color with RGBA support
-  Responsive to mouse enter, leave, focus, and blur events
-  Fade in/out animations for smooth transitions
-  TypeScript support with proper type definitions

## Props

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Prop</TableHead>
      <TableHead>Type</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><code>children</code></TableCell>
      <TableCell><code>React.ReactNode</code></TableCell>
      <TableCell><code>"Hover me"</code></TableCell>
      <TableCell>Content to display inside the spotlight container</TableCell>
    </TableRow>
    
    <TableRow>
      <TableCell><code>className</code></TableCell>
      <TableCell><code>string</code></TableCell>
      <TableCell><code>"h-52 w-40 flex justify-center items-center bg-black border border-white"</code></TableCell>
      <TableCell>CSS classes for styling the container</TableCell>
    </TableRow>
   
    <TableRow>
      <TableCell><code>spotlightColor</code></TableCell>
      <TableCell><code>string</code></TableCell>
      <TableCell><code>'rgba(151, 151, 151, 0.1)'</code></TableCell>
      <TableCell>Color of the spotlight effect in RGBA format</TableCell>
    </TableRow>
    
  </TableBody>
</Table>

## Color Examples

Here are some popular spotlight color combinations:

<CopyCode>
  {`// Subtle gray (default)
spotlightColor="rgba(151, 151, 151, 0.1)"

// Blue accent
spotlightColor="rgba(59, 130, 246, 0.15)"

// Purple glow
spotlightColor="rgba(168, 85, 247, 0.2)"

// Green highlight  
spotlightColor="rgba(34, 197, 94, 0.1)"

// Red warning
spotlightColor="rgba(239, 68, 68, 0.12)"

// Gold premium
spotlightColor="rgba(245, 158, 11, 0.15)"

// Cyan modern
spotlightColor="rgba(6, 182, 212, 0.1)"`}
</CopyCode>