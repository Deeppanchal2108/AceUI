---
title: Installation
description:  AceUI extends shadcn/ui with smooth animations, advanced components, and modern background patterns , crafted for standout UIs.
author: deep
published: true
date: 2025-07-16
slug: components/installation
---


<Steps>

### Create project

Run the `init` command to create a new Next.js project or to setup an existing one:

<CopyCode>
```bash
npx shadcn@latest init
```
</CopyCode>

### Add components

You can now start adding components to your project.

<CopyCode>
```bash
npx shadcn@latest add "http://localhost:3000/r/text-scramble.json"
```
</CopyCode>

### Import component

The command above will add the `TextScramble` component to your project. You can then import it like this:



<CopyCode>
```tsx 
import { TextScramble } from "@/components/ui/text-scramble";

export default function Home() {
  return (
    <div>
      <TextScramble/>
    </div>
  );
}
```

</CopyCode>

</Steps>